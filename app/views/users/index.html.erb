<body>
<div class="container">
  <div class="row">
    <div class="col-xs-3">
      <%= render 'list', user: @user %>
      <%= render 'books/form', book: @book %>
    </div>

    <div class="col-xs-9">
      <h2>Users</h2>
      <table class="table">
	    <thead>
		  <tr>
			<th>image</th>
			<th>name</th>
			<th></th>
		  </tr>
	    </thead>
		<% @users.each do |user| %>
		<tr>
	      <td><%= attachment_image_tag user, :profile_image, format: 'jpg', class: "pull-left profile-img", fallback: "no_image.jpg", size:"40×40" %></td>
		  <td><%= user.name %></td>
		  <td><%= link_to "show", user_path(user.id) %></td>
		</tr>
        <% end %>
       </table>
    </div>
  </div>
</div>
</body>
